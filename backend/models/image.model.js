const mongoose = require('mongoose')

const ImageSchema = mongoose.Schema({
  myFile:String
})

//export default mongoose.model.posts || mongoose.model('post', ImageSchema)
//exports.mongoose.model('imageModel',ImageSchema)